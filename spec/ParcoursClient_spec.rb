require "spec_helper"

describe ParcoursClient do
  it "has a version number" do
    expect(ParcoursClient::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
